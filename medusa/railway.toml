[build]
builder = "NIXPACKS"
buildCommand = "yarn install && yarn build"

[deploy]
preDeployCommand = "yarn medusa db:migrate"
startCommand = "yarn start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
